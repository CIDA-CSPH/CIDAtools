# Workflow derived from https://github.com/CIDA-CSPH/CIDAtools/pkgdown.yaml

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  release:
    types: [published]
  workflow_dispatch:

name: altdoc_quarto_site

jobs:
  build_doc:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
      matrix:
        r-version: ['release']
    # Only restrict concurrency for non-PR jobs
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-pandoc@v2

      - name: Set up R ${{ matrix.r-version }}
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: ${{ matrix.r-version }}
          use-public-rspm: true

      - name: Install dependencies
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: any::rcmdcheck,altdoc::render_docs,quarto
          needs: check
          
      - name: Cache R
        uses: actions/cache@v4
        with:
          path: ${{ env.R_LIBS_USER }}
          key: ${{ runner.os }}-r-${{ hashFiles('DESCRIPTION') }}

      - uses: r-lib/actions/check-r-package@v2
        with:
          upload-snapshots: true

      - name: Document
        run: Rscript -e 'roxygen2::roxygenise()'

      - name: Build site
        run: render_docs(path = ".", verbose = FALSE, parallel = FALSE, freeze = FALSE)
        shell: Rscript {0}

      - name: Deploy to GitHub pages 🚀
        if: github.event_name != 'pull_request'
        uses: JamesIves/github-pages-deploy-action@v4.4.1
        with:
          clean: false
          branch: gh-pages
          folder: docs
